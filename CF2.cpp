#include <bits/stdc++.h>
#define MAXN 200001
using namespace std;


int n,k;
int d[MAXN];
int visited[MAXN];
int res=0;

int main(){
  scanf("%d%d",&n,&k);
  for(int i=0;i<n;i++) scanf("%d",&d[i]);

  return 0;
}
